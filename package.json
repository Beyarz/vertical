{
  "name": "vertical",
  "version": "1.0.0",
  "description": "New verticle line every second",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "coffee -c -t -M script.coffee prepend.coffee"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4"
  },
  "dependencies": {
    "coffeescript": "^2.5.1"
  }
}
